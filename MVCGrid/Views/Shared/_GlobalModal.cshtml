<!-- Global Unified Modal for entire application - FIXED FOR INTERACTION -->
<div class="modal fade" id="globalModal" tabindex="-1" data-bs-backdrop="static" data-bs-keyboard="false" aria-labelledby="globalModalTitle" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-lg modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="globalModalTitle">
                    <i class="fas fa-circle-notch fa-spin modal-title-loader" id="modalTitleLoader"></i>
                    <span id="modalTitleText">Loading...</span>
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body" id="globalModalBody">
                <!-- Loading spinner -->
                <div class="text-center py-5" id="modalLoadingSpinner">
                    <div class="spinner-border text-primary" role="status" style="width: 3rem; height: 3rem;">
                        <span class="visually-hidden">Loading...</span>
                    </div>
                    <p class="mt-3 text-muted">Loading content...</p>
                </div>
                <!-- Content will be loaded here via AJAX -->
            </div>
            <div class="modal-footer" id="globalModalFooter" style="display: none;">
                <!-- Footer content loaded dynamically if needed -->
            </div>
        </div>
    </div>
</div>

<style>
    /* Global Modal - CRITICAL FIX FOR INTERACTION */
    
    /* Z-Index hierarchy - Modal MUST be highest */
    #globalModal {
        z-index: 11000 !important;
        pointer-events: auto !important; /* CRITICAL: Allow interaction */
    }
    
    .modal-backdrop {
        z-index: 10900 !important;
        background-color: rgba(0, 0, 0, 0.5) !important;
        pointer-events: auto !important; /* Backdrop can receive events */
    }
    
    /* Ensure modal dialog and content are interactive */
    #globalModal .modal-dialog {
        max-width: 700px;
        width: 90%;
        margin: 1.75rem auto;
        pointer-events: auto !important; /* CRITICAL */
    }
    
    #globalModal .modal-content {
        border: none;
        border-radius: 12px;
        box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
        overflow: visible; /* Allow dropdowns to overflow */
        pointer-events: auto !important; /* CRITICAL */
        opacity: 1 !important; /* Ensure visibility */
    }
    
    /* Header styling */
    #globalModal .modal-header {
        background: linear-gradient(135deg, #2563eb 0%, #1e40af 100%);
        color: white;
        border-radius: 12px 12px 0 0;
        padding: 20px 25px;
        border-bottom: none;
        pointer-events: auto !important;
    }
    
    #globalModal .modal-title {
        font-size: 1.25rem;
        font-weight: 600;
        margin: 0;
    }
    
    .modal-title-loader {
        margin-right: 10px;
        display: inline-block;
    }
    
    .modal-title-loader.hidden {
        display: none !important;
    }
    
    #globalModal .btn-close {
        filter: brightness(0) invert(1);
        opacity: 0.8;
        pointer-events: auto !important; /* CRITICAL */
    }
    
    #globalModal .btn-close:hover {
        opacity: 1;
    }
    
    /* Body - Ensure all interactions work */
    #globalModal .modal-body {
        padding: 25px;
        min-height: 150px;
        max-height: calc(100vh - 250px);
        overflow-y: auto;
        overflow-x: hidden;
        pointer-events: auto !important; /* CRITICAL */
    }
    
    /* All form elements MUST be interactive */
    #globalModal .modal-body * {
        pointer-events: auto !important; /* CRITICAL */
    }
    
    #globalModal .modal-footer {
        border-top: 1px solid #e5e7eb;
        padding: 15px 25px;
        background: #f9fafb;
        pointer-events: auto !important;
    }
    
    /* Smooth animations */
    #globalModal.fade .modal-dialog {
        transition: transform 0.3s ease-out, opacity 0.3s ease-out;
        transform: scale(0.9);
        opacity: 0;
    }
    
    #globalModal.show .modal-dialog {
        transform: scale(1);
        opacity: 1;
    }
    
    /* Loading spinner animation */
    #modalLoadingSpinner {
        animation: fadeIn 0.3s ease-in;
    }
    
    @@keyframes fadeIn {
        from { opacity: 0; }
        to { opacity: 1; }
    }
    
    /* Form styling - Ensure all inputs are interactive */
    #globalModal .form-label {
        font-weight: 600;
        color: #374151;
        margin-bottom: 8px;
        font-size: 0.95rem;
        pointer-events: none; /* Labels don't need events */
    }
    
    #globalModal .form-control,
    #globalModal .form-select,
    #globalModal input,
    #globalModal select,
    #globalModal textarea,
    #globalModal button {
        border-radius: 6px;
        border: 1px solid #d1d5db;
        padding: 10px 12px;
        font-size: 1rem;
        pointer-events: auto !important; /* CRITICAL */
        cursor: text; /* Show text cursor */
    }
    
    #globalModal button {
        cursor: pointer; /* Show pointer cursor */
    }
    
    #globalModal .form-control:focus,
    #globalModal .form-select:focus,
    #globalModal input:focus,
    #globalModal select:focus,
    #globalModal textarea:focus {
        border-color: #2563eb;
        box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.1);
        outline: none;
        pointer-events: auto !important; /* CRITICAL */
    }
    
    /* Button styling */
    #globalModal .btn {
        padding: 10px 20px;
        border-radius: 6px;
        font-weight: 500;
        font-size: 1rem;
        pointer-events: auto !important; /* CRITICAL */
        cursor: pointer;
    }
    
    #globalModal .btn-primary {
        background: linear-gradient(135deg, #2563eb 0%, #1e40af 100%);
        border: none;
    }
    
    #globalModal .btn-primary:hover {
        transform: translateY(-1px);
        box-shadow: 0 4px 12px rgba(37, 99, 235, 0.3);
    }
    
    #globalModal .btn-success {
        background: linear-gradient(135deg, #10b981 0%, #059669 100%);
        border: none;
    }
    
    #globalModal .btn-success:hover {
        transform: translateY(-1px);
        box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3);
    }
    
    /* Responsive modal */
    @@media (max-width: 768px) {
        #globalModal .modal-dialog {
            max-width: 95%;
            margin: 0.5rem auto;
        }
        
        #globalModal .modal-body {
            padding: 20px;
            max-height: calc(100vh - 200px);
        }
    }
    
    /* Ensure modal is always visible and layered properly */
    #globalModal.show {
        display: block !important;
        pointer-events: auto !important; /* CRITICAL */
    }
    
    /* Fix for any layout conflicts */
    #globalModal * {
        box-sizing: border-box;
    }
    
    /* Ensure dropdown menus work */
    #globalModal .dropdown-menu {
        z-index: 11100 !important;
        pointer-events: auto !important;
    }
    
    /* Ensure select dropdowns work */
    #globalModal select {
        pointer-events: auto !important;
        cursor: pointer;
    }
</style>
